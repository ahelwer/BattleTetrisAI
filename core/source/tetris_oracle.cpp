#include <core/tetris_oracle.hpp>
#include <core/game_state_eval.hpp>

Tetronimo const* FindBestMove(GameState& state, Harmony const& h) {
    return NULL;
}

std::vector<Tetronimo> const* FindPossibleMoves(GameState& state) {
    std::vector<Tetronimo>* moves = new std::vector<Tetronimo>();
    return moves;
}

